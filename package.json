{
  "name": "httpoint",
  "version": "1.1.0",
  "description": "A Node.js command-line application that serves static files from a local directory via HTTP and supports file uploads",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dodbrian/httpoint.git"
  },
  "main": "dist/serve.js",
  "bin": {
    "httpoint": "dist/serve.js"
  },
  "scripts": {
    "build": "npm run lint && tsc && node scripts/copy-assets.js",
    "start": "node dist/serve.js",
    "dev": "npm run build && node dist/serve.js",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "http",
    "server",
    "static",
    "files",
    "upload",
    "cli",
    "local"
  ],
  "author": "Denis Zimin",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@typescript-eslint/eslint-plugin": "^8.53.0",
    "@typescript-eslint/parser": "^8.53.0",
    "eslint": "^9.39.2",
    "typescript": "^5.7.2"
  }
}
